/**
 * Configuration object for book-generator
 *
 * @type {{minBatchSize: number, maxBatchSize: number, defaultBooksRequest: number}}
 */
const config = {
  minBatchSize: 1,
  maxBatchSize: 300,
  defaultBooksRequest: 1000000,
};

export default config;
